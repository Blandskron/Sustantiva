# Importamos la función saludar desde el módulo saludar.
from saludar import saludar

# Llamamos a la función saludar y pasamos el argumento "Pepo".
# La función debería devolver un saludo utilizando el nombre "Pepo".
# Luego, el resultado se imprime en la consola.
print(saludar("Pepo"))
